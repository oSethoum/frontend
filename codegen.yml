overwrite: true
schema: "http://localhost:4000"
documents: "src/**/*.graphql"
generates:
  src/graphql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "urql-introspection"
