overwrite: true
schema: "http://localhost:3001/query"
documents: "src/**/*.graphql"
generates:
  src/graphql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "urql-introspection"
      - "typescript-urql"
